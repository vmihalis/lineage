// Generation module -- lifecycle state machine and multi-generation simulation loop.
// Orchestrates all subsystems (roles, mortality, interaction, transmission, mutation, inheritance).

export { runGeneration } from './generation-runner.js';
export { runSimulation } from './simulation-runner.js';
