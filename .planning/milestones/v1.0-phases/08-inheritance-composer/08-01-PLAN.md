---
phase: 08-inheritance-composer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inheritance/transmission-reader.ts
  - src/inheritance/seed-layer.ts
  - src/inheritance/recent-layer.ts
  - src/inheritance/inheritance.test.ts
autonomous: true
requirements: [INHR-01, INHR-02, INHR-03]

must_haves:
  truths:
    - "Transmissions can be read from disk by generation number"
    - "Missing generation directories return empty arrays, not errors"
    - "Seed compression prompt groups tokens by generation with provenance"
    - "Seed layer formatted text starts with ANCESTRAL KNOWLEDGE header"
    - "Recent layer formatted text includes role and citizenId context per transmission"
    - "Empty transmissions produce empty string, not errors"
  artifacts:
    - path: "src/inheritance/transmission-reader.ts"
      provides: "Disk I/O for reading transmission JSON files by generation"
      exports: ["readGenerationTransmissions", "readAllPriorTransmissions"]
    - path: "src/inheritance/seed-layer.ts"
      provides: "Seed compression prompt builder and seed layer formatter"
      exports: ["SEED_COMPRESSION_SYSTEM_PROMPT", "buildSeedCompressionPrompt", "formatSeedLayer"]
    - path: "src/inheritance/recent-layer.ts"
      provides: "Recent layer formatting from single generation transmissions"
      exports: ["formatRecentLayer"]
    - path: "src/inheritance/inheritance.test.ts"
      provides: "Tests for all pure functions in the inheritance module"
      min_lines: 150
  key_links:
    - from: "src/inheritance/transmission-reader.ts"
      to: "src/schemas/transmission.ts"
      via: "TransmissionSchema.parse for validation on read"
      pattern: "TransmissionSchema\\.parse"
    - from: "src/inheritance/seed-layer.ts"
      to: "src/transmission/anchor-parser.ts"
      via: "extractAnchorTokens used by executor (Plan 02) on compression output"
      pattern: "extractAnchorTokens"
    - from: "src/inheritance/recent-layer.ts"
      to: "src/schemas/transmission.ts"
      via: "Transmission type for input parameter"
      pattern: "Transmission\\[\\]"
---

<objective>
Create the pure function layer of the inheritance composer: disk reading, prompt building, and text formatting.

Purpose: These pure functions are the data pipeline that reads transmissions from disk, builds prompts for LLM-powered seed compression, and formats both seed and recent layers as structured text for delivery to new citizens. Plan 02 will wire these to the Agent SDK executor and orchestrator.

Output: Four source files (transmission-reader.ts, seed-layer.ts, recent-layer.ts) and a comprehensive test file covering all pure function behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-inheritance-composer/08-RESEARCH.md

@src/schemas/transmission.ts
@src/schemas/simulation.ts
@src/transmission/anchor-parser.ts
@src/transmission/transmission-writer.ts
@src/mutation/mutation.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/schemas/transmission.ts:
```typescript
export const TransmissionSchema = z.object({
  id: z.string(),
  citizenId: z.string(),
  generationNumber: z.number().int().positive(),
  role: CitizenRoleSchema,
  type: TransmissionTypeSchema,
  content: z.string(),
  anchorTokens: z.array(z.string()).default([]),
  timestamp: z.string().datetime(),
  mutated: z.boolean().default(false),
  mutationType: z.string().optional(),
});
export type Transmission = z.infer<typeof TransmissionSchema>;
```

From src/schemas/simulation.ts:
```typescript
export const SimulationParametersSchema = z.object({
  // ...
  inheritanceStagingRates: z.object({
    seedLayerAtBirth: z.boolean().default(true),
    recentLayerThreshold: z.number().min(0).max(1).default(0.25),
  }).default({ seedLayerAtBirth: true, recentLayerThreshold: 0.25 }),
  outputDir: z.string().default('./output'),
});
```

From src/transmission/anchor-parser.ts:
```typescript
export function extractAnchorTokens(text: string): string[];
```

File path convention (from transmission-writer.ts):
```
{outputDir}/transmissions/gen{N}/{citizenId}-{type}.json
```

From src/events/types.ts:
```typescript
'inheritance:composed': (generationNumber: number, layerCount: number) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transmission reader and inheritance test scaffold</name>
  <files>src/inheritance/transmission-reader.ts, src/inheritance/inheritance.test.ts</files>
  <read_first>
    src/schemas/transmission.ts
    src/transmission/transmission-writer.ts
    src/mutation/mutation.test.ts
    src/schemas/simulation.ts
  </read_first>
  <behavior>
    - readGenerationTransmissions(outputDir, 1) reads all .json files from {outputDir}/transmissions/gen1/, parses each with TransmissionSchema.parse, returns Transmission[]
    - readGenerationTransmissions returns empty array when generation directory does not exist (ENOENT), not an error
    - readGenerationTransmissions skips non-.json files in the directory
    - readAllPriorTransmissions(outputDir, 3) reads gen1 and gen2 (NOT gen3), returns combined Transmission[]
    - readAllPriorTransmissions(outputDir, 1) returns empty array (no prior generations)
  </behavior>
  <action>
    Create `src/inheritance/transmission-reader.ts` with two exported async functions:

    1. `readGenerationTransmissions(outputDir: string, generationNumber: number): Promise<Transmission[]>`
       - Constructs path: `join(outputDir, 'transmissions', \`gen\${generationNumber}\`)`
       - Uses `readdir(genDir)` wrapped in try-catch -- catch returns `[]` (handles ENOENT for missing directories)
       - Filters files to only `.json` suffix
       - For each .json file: `readFile(join(genDir, file), 'utf-8')`, `JSON.parse()`, `TransmissionSchema.parse()`
       - Returns the collected `Transmission[]` array
       - Imports: `{ readdir, readFile } from 'node:fs/promises'`, `{ join } from 'node:path'`, `{ TransmissionSchema } from '../schemas/index.js'`, `type { Transmission } from '../schemas/index.js'`

    2. `readAllPriorTransmissions(outputDir: string, upToGeneration: number): Promise<Transmission[]>`
       - If `upToGeneration <= 1`, return `[]` (no prior generations for gen 1)
       - Loop from `gen = 1` to `gen < upToGeneration`, calling `readGenerationTransmissions` for each
       - Concatenate all results into a single `Transmission[]` array
       - Returns the combined array

    Create `src/inheritance/inheritance.test.ts` with test scaffold:
    - Mock `node:fs/promises` using `vi.mock('node:fs/promises')` with `readdir` and `readFile` mocks
    - Create `makeTransmission(overrides)` helper (same pattern as mutation.test.ts `makeTransmission`)
    - Write tests for all behaviors listed above
    - Use `JSON.stringify(TransmissionSchema.parse({...}))` as mock readFile return values to ensure realistic JSON
    - Test ENOENT handling by having mock readdir throw `{ code: 'ENOENT' }` error object

    Agent SDK mock pattern (from mutation.test.ts): Place `vi.mock(...)` calls before imports that use the mocked module.
  </action>
  <verify>
    <automated>cd /Users/memehalis/lineage && npx vitest run src/inheritance/inheritance.test.ts -t "readGenerationTransmissions|readAllPriorTransmissions"</automated>
  </verify>
  <acceptance_criteria>
    - src/inheritance/transmission-reader.ts contains `export async function readGenerationTransmissions(`
    - src/inheritance/transmission-reader.ts contains `export async function readAllPriorTransmissions(`
    - src/inheritance/transmission-reader.ts contains `TransmissionSchema.parse(`
    - src/inheritance/transmission-reader.ts contains `} catch` for ENOENT handling
    - src/inheritance/transmission-reader.ts contains `file.endsWith('.json')` filter
    - src/inheritance/inheritance.test.ts contains `vi.mock('node:fs/promises'`
    - src/inheritance/inheritance.test.ts contains `describe('readGenerationTransmissions'`
    - src/inheritance/inheritance.test.ts contains `describe('readAllPriorTransmissions'`
    - npx vitest run src/inheritance/inheritance.test.ts exits with code 0
  </acceptance_criteria>
  <done>Both readGenerationTransmissions and readAllPriorTransmissions pass all tests including ENOENT handling and generation range filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed layer prompt builder, seed formatter, and recent layer formatter</name>
  <files>src/inheritance/seed-layer.ts, src/inheritance/recent-layer.ts, src/inheritance/inheritance.test.ts</files>
  <read_first>
    src/inheritance/transmission-reader.ts
    src/inheritance/inheritance.test.ts
    src/transmission/anchor-parser.ts
    src/interaction/handoff.ts
    src/mutation/mutation-prompts.ts
    src/schemas/transmission.ts
  </read_first>
  <behavior>
    - buildSeedCompressionPrompt with 2 generations of tokens returns string containing "Generation 1 transmitted:" and "Generation 2 transmitted:"
    - buildSeedCompressionPrompt includes all token text from each generation
    - buildSeedCompressionPrompt contains "3-5 essential claims" instruction
    - buildSeedCompressionPrompt contains "[1]" format instruction for anchor token compatibility
    - buildSeedCompressionPrompt contains "Return ONLY" no-preamble instruction
    - SEED_COMPRESSION_SYSTEM_PROMPT is a non-empty string containing "civilization" or "memory"
    - formatSeedLayer with 3 tokens returns string starting with "ANCESTRAL KNOWLEDGE"
    - formatSeedLayer with empty array returns empty string
    - formatSeedLayer includes all provided tokens prefixed with "- "
    - formatSeedLayer includes generation count in header
    - formatRecentLayer with 2 transmissions returns string starting with "INHERITANCE FROM GENERATION"
    - formatRecentLayer includes role and citizenId context per transmission using "--- role (citizen citizenId) ---" header format
    - formatRecentLayer includes each transmission's anchor tokens prefixed with "- "
    - formatRecentLayer with empty array returns empty string
    - formatRecentLayer ends with a guidance instruction for the receiving citizen
  </behavior>
  <action>
    Create `src/inheritance/seed-layer.ts` with three exports:

    1. `SEED_COMPRESSION_SYSTEM_PROMPT` constant string:
       ```
       You are the collective memory of a civilization.
       Knowledge from multiple generations has been transmitted to you. Some ideas appear repeatedly
       across generations -- these are the civilization's most durable wisdom. Some ideas appear only
       once -- these may be recent or may have been lost. Your task is to identify and compress the
       most enduring knowledge into a small number of essential claims. Output ONLY numbered claims
       in [N] format.
       ```

    2. `buildSeedCompressionPrompt(tokensByGeneration: Map<number, string[]>, targetGeneration: number): string`
       - Opens with `--- CIVILIZATION KNOWLEDGE ARCHIVE ---` header
       - Includes explanatory line: `The following is the accumulated transmitted knowledge from ${tokensByGeneration.size} generation(s) of civilization, being prepared for generation ${targetGeneration}.`
       - For each entry in tokensByGeneration (ordered by generation number):
         - `Generation ${gen} transmitted:`
         - Each token as `  - ${token}`
       - Closes with instructions:
         - `Compress the most enduring, most repeated ideas into 3-5 essential claims.`
         - `Ideas that appear across multiple generations are more durable than ideas from a single generation.`
         - `Format as numbered claims: [1] ... [2] ... etc.`
         - `Return ONLY the numbered claims. No preamble, no explanation.`
       - Lines joined with `\n`

    3. `formatSeedLayer(seedTokens: string[], generationCount: number): string`
       - If `seedTokens.length === 0`, return `''`
       - Opens with `ANCESTRAL KNOWLEDGE (distilled from ${generationCount} generation(s)):`
       - Second line: `The following represents the oldest, most enduring wisdom of your civilization.`
       - Blank line
       - Each token as `- ${token}`
       - Blank line
       - Closing: `This knowledge has survived across generations. Treat it as your civilization's foundation.`
       - Lines joined with `\n`

    Create `src/inheritance/recent-layer.ts` with one export:

    1. `formatRecentLayer(recentTransmissions: Transmission[], fromGeneration: number): string`
       - Import `type { Transmission } from '../schemas/index.js'`
       - If `recentTransmissions.length === 0`, return `''`
       - Opens with `INHERITANCE FROM GENERATION ${fromGeneration}:`
       - Second line: `The following knowledge was transmitted by citizens of the previous generation.`
       - Blank line
       - For each transmission:
         - `--- ${tx.role} (citizen ${tx.citizenId.slice(0, 8)}) ---`
         - Each anchorToken as `- ${token}`
         - Blank line after each transmission
       - Closing: `Consider this inherited knowledge alongside the seed problem. Build on it, question it, or preserve it based on your role.`
       - Lines joined with `\n`

    Add tests to `src/inheritance/inheritance.test.ts` for all behaviors listed above. Tests are pure function tests -- no mocks needed for these (only the transmission reader tests need fs mocks).
  </action>
  <verify>
    <automated>cd /Users/memehalis/lineage && npx vitest run src/inheritance/inheritance.test.ts -t "buildSeedCompressionPrompt|SEED_COMPRESSION_SYSTEM_PROMPT|formatSeedLayer|formatRecentLayer"</automated>
  </verify>
  <acceptance_criteria>
    - src/inheritance/seed-layer.ts contains `export const SEED_COMPRESSION_SYSTEM_PROMPT`
    - src/inheritance/seed-layer.ts contains `export function buildSeedCompressionPrompt(`
    - src/inheritance/seed-layer.ts contains `export function formatSeedLayer(`
    - src/inheritance/seed-layer.ts contains `3-5 essential claims`
    - src/inheritance/seed-layer.ts contains `Return ONLY`
    - src/inheritance/seed-layer.ts contains `ANCESTRAL KNOWLEDGE`
    - src/inheritance/recent-layer.ts contains `export function formatRecentLayer(`
    - src/inheritance/recent-layer.ts contains `INHERITANCE FROM GENERATION`
    - src/inheritance/recent-layer.ts contains `tx.citizenId.slice(0, 8)`
    - src/inheritance/inheritance.test.ts contains `describe('buildSeedCompressionPrompt'`
    - src/inheritance/inheritance.test.ts contains `describe('formatSeedLayer'`
    - src/inheritance/inheritance.test.ts contains `describe('formatRecentLayer'`
    - npx vitest run src/inheritance/inheritance.test.ts exits with code 0
  </acceptance_criteria>
  <done>Seed compression prompt builder, seed layer formatter, and recent layer formatter all pass tests with correct output format matching the established handoff.ts and mutation-prompts.ts patterns.</done>
</task>

</tasks>

<verification>
All inheritance pure functions tested:
```bash
npx vitest run src/inheritance/inheritance.test.ts
```

Full project test suite still passes:
```bash
npx vitest run
```

TypeScript compiles without errors:
```bash
npx tsc --noEmit
```
</verification>

<success_criteria>
- readGenerationTransmissions reads .json files from correct path, validates with TransmissionSchema, handles ENOENT
- readAllPriorTransmissions correctly reads gen 1 through gen N-1
- buildSeedCompressionPrompt produces prompt with generation-grouped tokens and [N] format instruction
- formatSeedLayer produces ANCESTRAL KNOWLEDGE header with token list
- formatRecentLayer produces INHERITANCE FROM GENERATION header with role/citizen context
- All empty/edge cases return empty strings or empty arrays (no errors)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-inheritance-composer/08-01-SUMMARY.md`
</output>
